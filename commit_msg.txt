chore(release): Bump to 0.2.0 with request coordination layer

New features:
- MonetixRequestCoordinator — global request burst prevention with priority
  queue, per-type spacing, deduplication, and metrics.
- Native fallback no longer pre-emptively double-requests on latency.
- Interstitial reload uses jittered cooldown with cancellable timer.
- Gate cascade debouncing prevents synchronized widget re-evaluation.
- Debug panel now inherits host color scheme, has full mutation toggles
  and live 50-entry timeline log.
- Debug interface hooks (setPremiumForDebug, setAdsEnabledForDebug, etc.)
  for any IAdStatusProvider / IAdConfigProvider implementation.
- Monetix.wire() for Provider-heavy setups.
- Monetix.debugMetrics() for observability.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
