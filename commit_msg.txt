refactor(example): Unify duplicate debug panels

The example app had two debug panels — DebugPanelScreen (AppBar button)
and MonetixDebugPanel (FAB). Both served the same purpose but with
different UIs and capabilities.

Changes:
- Point AppBar button at MonetixDebugPanel and remove the FAB
- Delete the now-unused DebugPanelScreen
- Remove unused debug_panel_screen import from home_screen.dart

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
